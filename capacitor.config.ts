import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ProyectoApiRest5V',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
